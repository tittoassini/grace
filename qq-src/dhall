let t = λ(union : < Left : Natural | Right : Bool >) → merge { Left = Natural/even, Right = λ(b : Bool) → b } union : Bool
in t < Left=13 | Right:Bool >

let t : (Left:Natural | Right Bool) = {
  Left l -> even l
  Right r -> r  
} in t (Left 13)
